<%= form_with(model: @menu, local: true) do |form| %>

  <div class="time">
    <%= form.label :cook_date %>
    <%= form.date_select :cook_date %>
  </div>

  <div class="submit">
    <%= form.submit %>
  </div>

<% end %>